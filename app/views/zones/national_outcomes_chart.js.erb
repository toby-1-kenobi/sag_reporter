$('#national-progress-chart').html(
    '<%= j(line_chart Zone.national_outcome_chart_data,
                  height: '180px',
                  legend: false,
                  library: {
                    backgroundColor: "transparent",
                    legend: {position: 'none'},
                    hAxis: { textPosition: 'none' },
                    vAxis: { textPosition: 'none' }
                  }
          ) %>'
);