
<% if @languages.count == 1 %>
<% lang = @languages.first.first %>
<% last_update = @languages.first.second %>
You are the primary point person for the <%= lang.name %> language in LCI. This language hasn't seen any updates for <%= time_ago_in_words(last_update) %>.

Please log into the <%= link_to 'LCI App', login_url %> and go to the <%= link_to "#{lang.name} details page", show_details_language_url(lang.id) %>
<% else %>
You are the primary point person for each of the following languages. These particular languages haven't seen an update in a while.
<% @languages.each do |lang, last_updated| %>
  * <%= link_to "#{lang.name} details page", show_details_language_url(lang.id) %>: last updated <%= time_ago_in_words(last_updated) %> ago
<% end %>

Please log into the <%= link_to 'LCI App', login_url %> and go to the details page of each of these languages to check they are up to date.
<% end %>

If you find any information that needs to be changed you may make an edit on the language details page.

This email was automatically generated from the LCI App. If you feel you should not have received it please contact your LCI supervisor or replay to this email.
