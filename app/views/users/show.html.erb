<% provide(:title, "Show User: #{@user.id}") %>
<%= render partial: "user_card", object: @user %>
<% if logged_in_user.admin? %>
    <%= link_to "#{@user.reports.active.count} reports", reports_user_path(@user) %>
<% end %>
