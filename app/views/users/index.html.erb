<% provide(:title, 'All users') %>
<h1><i class="mdi-social-people"></i>
 All users</h1>

<%= will_paginate %>

<ul class="users collection">
  <% @users.each do |user| %>
    <%= render @users %>
  <% end %>
</ul>

<%= will_paginate %>
